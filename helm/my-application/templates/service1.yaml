apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.service1.name }}
spec:
  selector:
    app: {{ .Values.services.service1.name }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.services.service1.port }}
