apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.service3.name }}
spec:
  selector:
    app: {{ .Values.services.service3.name }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.services.service3.port }}
